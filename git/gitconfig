[user]
    name = Michelle Laurenti
    email = asmeikal@me.com
[color]
    ui = true
    status = auto
[diff]
    tool = vimdiff
    algorithm = patience
    submodule = log
[core]
    editor = vim
    autocrlf = input
    excludesfile = ~/.gitignore_global
    hookPath = ~/.githooks

[push]
    default = simple

[alias]
    st = status
    ci = commit
    br = branch -vv --list
    bra = branch -vv --list --all
    dc = diff --cached
    ls = log --graph --decorate --oneline --all
    cl = clean -xi
    last = log -1 HEAD
    whoami = config user.email

[includeIf "gitdir:~/Developer/chainside/"]
    path = ~/Developer/chainside/.gitconfig

[includeIf "gitdir:~/Developer/moveax/"]
    path = ~/Developer/moveax/.gitconfig

